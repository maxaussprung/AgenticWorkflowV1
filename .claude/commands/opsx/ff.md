---
name: "OPSX: Fast Forward"
description: Create an OpenSpec change and generate all artifacts needed for implementation in one pass (Experimental)
category: Workflow
tags: [workflow, artifacts, experimental]
---

Use the canonical workflow in `.agents/skills/openspec-ff-change/SKILL.md`.

Before acting:

1. Read `.agents/skills/openspec-ff-change/SKILL.md` completely.
2. Treat this command's arguments as the skill input.
3. When the skill mentions `/opsx:ff`, treat that as this Claude Code command.

Do not copy or restate the fast-forward workflow here; `.agents/skills/openspec-ff-change/SKILL.md` is the source of truth.
