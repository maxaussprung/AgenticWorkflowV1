# Frontend Source

This folder holds the target application's frontend source code.

For agent guidance on working with the frontend code, see [AGENTS.md](AGENTS.md).

## Structure

Place your frontend project files here. Recommended structure:

- `src/` — TypeScript/React source
- `public/` — Static assets
- `Dockerfile` — Container build (already provided as template)
