---
name: "OPSX: Propose"
description: Create an OpenSpec change with proposal, design, specs, and tasks
category: Workflow
tags: [workflow, artifacts, experimental]
---

Use the canonical workflow in `.agents/skills/openspec-propose/SKILL.md`.

Before acting:

1. Read `.agents/skills/openspec-propose/SKILL.md` completely.
2. Treat this command's arguments as the skill input.
3. When the skill mentions `/opsx:propose`, treat that as this Claude Code command.

Do not copy or restate the propose workflow here; `.agents/skills/openspec-propose/SKILL.md` is the source of truth.
