---
description: Implement tasks from an OpenSpec change (Experimental)
---

Use the canonical workflow in `.agents/skills/openspec-apply-change/SKILL.md`.

Before acting:

1. Read `.agents/skills/openspec-apply-change/SKILL.md` completely.
2. Treat this command's arguments as the skill input.
3. When the skill mentions `/opsx:apply`, treat that as `/opsx-apply` in OpenCode.

Do not copy or restate the implementation workflow here; `.agents/skills/openspec-apply-change/SKILL.md` is the source of truth.
