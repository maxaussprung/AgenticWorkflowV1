# <Topic name> — <one-line scope: when to read this file>

> TEMPLATE — copy this file to `NN-<topic>.md`, fill it with real, verified content, and add an
> index row in [README.md](README.md). Delete sections that don't apply; never keep empty ones.

## <Recipe: the verified way to do X>

State the goal, then the exact commands/steps — agent-actionable, not prose:

```bash
# exact command(s), verified working, with the flags that matter
```

Why it's done this way / what breaks otherwise (one or two lines max).

## GOTCHA: <symptom you'll see>

- **Symptom:** what the agent observes (error text, wrong behavior — use words a future grep will find).
- **Root cause:** why it happens.
- **Fix:** the exact working resolution.

## <Facts: stable project/tool constants worth not re-deriving>

| Thing | Value | Notes |
|---|---|---|
| … | … | verified <date> |

## Related tools

- [`tools/<script>`](tools/README.md) — link any script that serves this topic, in the same edit
  that adds it.
