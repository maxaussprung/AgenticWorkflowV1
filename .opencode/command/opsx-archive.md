---
description: Archive a completed OpenSpec change (Experimental)
---

Use the canonical workflow in `.agents/skills/openspec-archive-change/SKILL.md`.

Before acting:

1. Read `.agents/skills/openspec-archive-change/SKILL.md` completely.
2. Treat this command's arguments as the skill input.
3. When the skill mentions `/opsx:archive`, treat that as `/opsx-archive` in OpenCode.

Do not copy or restate the archive workflow here; `.agents/skills/openspec-archive-change/SKILL.md` is the source of truth.
