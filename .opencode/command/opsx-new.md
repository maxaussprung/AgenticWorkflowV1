---
description: Start a new OpenSpec change using the experimental artifact workflow
---

Use the canonical workflow in `.agents/skills/openspec-new-change/SKILL.md`.

Before acting:

1. Read `.agents/skills/openspec-new-change/SKILL.md` completely.
2. Treat this command's arguments as the skill input.
3. When the skill mentions `/opsx:new`, treat that as `/opsx-new` in OpenCode.

Do not copy or restate the new-change workflow here; `.agents/skills/openspec-new-change/SKILL.md` is the source of truth.
