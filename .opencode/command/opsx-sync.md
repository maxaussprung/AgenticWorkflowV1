---
description: Sync delta specs from an OpenSpec change to main specs
---

Use the canonical workflow in `.agents/skills/openspec-sync-specs/SKILL.md`.

Before acting:

1. Read `.agents/skills/openspec-sync-specs/SKILL.md` completely.
2. Treat this command's arguments as the skill input.
3. When the skill mentions `/opsx:sync`, treat that as `/opsx-sync` in OpenCode.

Do not copy or restate the sync workflow here; `.agents/skills/openspec-sync-specs/SKILL.md` is the source of truth.
