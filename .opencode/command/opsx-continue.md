---
description: Continue an OpenSpec change by creating the next artifact (Experimental)
---

Use the canonical workflow in `.agents/skills/openspec-continue-change/SKILL.md`.

Before acting:

1. Read `.agents/skills/openspec-continue-change/SKILL.md` completely.
2. Treat this command's arguments as the skill input.
3. When the skill mentions `/opsx:continue`, treat that as `/opsx-continue` in OpenCode.

Do not copy or restate the continue workflow here; `.agents/skills/openspec-continue-change/SKILL.md` is the source of truth.
