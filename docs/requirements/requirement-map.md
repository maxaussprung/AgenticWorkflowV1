---
title: "Technical view"
type: requirement-map
---

# Technical view

The graph below starts at products, flows through features, and ends at requirements. Requirement-to-requirement arrows are rendered from explicit `depends_on` links in requirement frontmatter.

Direction is prerequisite → dependent requirement. Independent requirements are hidden by default so the first view stays focused on sequencing and implementation dependencies.
