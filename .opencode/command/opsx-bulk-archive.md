---
description: Archive multiple completed OpenSpec changes at once (Experimental)
---

Use the canonical workflow in `.agents/skills/openspec-bulk-archive-change/SKILL.md`.

Before acting:

1. Read `.agents/skills/openspec-bulk-archive-change/SKILL.md` completely.
2. Treat this command's arguments as the skill input.
3. When the skill mentions `/opsx:bulk-archive`, treat that as `/opsx-bulk-archive` in OpenCode.

Do not copy or restate the bulk-archive workflow here; `.agents/skills/openspec-bulk-archive-change/SKILL.md` is the source of truth.
