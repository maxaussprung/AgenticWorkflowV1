---
description: Verify an implementation against OpenSpec change artifacts before archiving
---

Use the canonical workflow in `.agents/skills/openspec-verify-change/SKILL.md`.

Before acting:

1. Read `.agents/skills/openspec-verify-change/SKILL.md` completely.
2. Treat this command's arguments as the skill input.
3. When the skill mentions `/opsx:verify`, treat that as `/opsx-verify` in OpenCode.

Do not copy or restate the verify workflow here; `.agents/skills/openspec-verify-change/SKILL.md` is the source of truth.
